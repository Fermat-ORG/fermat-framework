<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="130dp"
    android:layout_marginBottom="0dp"
    android:layout_marginTop="0dp"
    android:background="@drawable/barra_contactos"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="0dp">

    <ImageView
        android:id="@+id/crypto_customer_identity_image"
        android:layout_width="95dp"
        android:layout_height="95dp"
        android:layout_marginLeft="18dp"
        android:layout_marginTop="-5dp"
        android:contentDescription="@string/identity_img"
        android:padding="0dp"
        android:scaleType="fitXY"
        tools:src="@drawable/no_picture_icon" />

    <com.bitdubai.fermat_android_api.layer.definition.wallet.views.FermatTextView
        android:id="@+id/crypto_customer_identity_alias"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="110dp"
        android:padding="10dp"
        android:singleLine="true"
        android:textColor="#ffffff"
        android:textSize="18sp"
        tools:text="@string/crypto_customer" />

</RelativeLayout>