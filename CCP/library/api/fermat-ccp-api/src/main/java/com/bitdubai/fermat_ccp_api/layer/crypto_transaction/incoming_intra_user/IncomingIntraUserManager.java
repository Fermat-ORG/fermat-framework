package com.bitdubai.fermat_ccp_api.layer.crypto_transaction.incoming_intra_user;

import com.bitdubai.fermat_api.layer.all_definition.common.system.interfaces.FermatManager;

/**
 * The interface <code>IncomingIntraUserManager</code>
 * define the methods that the Incoming Intra User plugin exposes to its clients. As this plugin has
 * no known client or passive service to expose, the interface is empty.
 *
 * @author Ezequiel Postan
 * @since Java JDK 1.7
 */
public interface IncomingIntraUserManager extends FermatManager {
}
