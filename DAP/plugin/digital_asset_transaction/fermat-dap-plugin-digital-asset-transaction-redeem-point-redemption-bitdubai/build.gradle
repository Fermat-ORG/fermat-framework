apply plugin: 'java'

sourceCompatibility = 1.7

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':fermat-pip-api')
    compile project(':fermat-dmp-plugin-middleware-bank-notes-bitdubai')
}